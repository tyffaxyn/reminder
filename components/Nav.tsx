import { CalendarClock } from 'lucide-react'

const Nav = () => {
  return (
    <nav className="border-b border-default-50 flex items-center py-2 px-6 gap-4">
      <CalendarClock /> Reminder
    </nav>
  )
}

export default Nav

